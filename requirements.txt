# Smart Mirror Pi — Python dependencies
# Install with: pip install -r requirements.txt

# Qt/QML UI framework
PySide6>=6.6.0

# (All other runtime tools are system packages — see README for apt-get list)
